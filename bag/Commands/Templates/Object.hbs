using System;

namespace {{Namespace}}
{
    public class {{ClassName}} : {{InterfaceList}}
    {
        public {{ClassName}}(IdHost idHost) : base(idHost)
        {
{{#each Properties}}
{{> AddProperty }}
{{/each}}
        }

{{#each Properties}}
{{> ClassProperty }}
{{/each}}

    }
}